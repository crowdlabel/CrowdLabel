/**
 * CrowdLabelAPI
 * API for CrowdLabel
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TasksResponse model module.
 * @module model/TasksResponse
 * @version 0.1.0
 */
class TasksResponse {
    /**
     * Constructs a new <code>TasksResponse</code>.
     * @alias module:model/TasksResponse
     * @param tasks {Array.<Number>} 
     */
    constructor(tasks) { 
        
        TasksResponse.initialize(this, tasks);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tasks) { 
        obj['tasks'] = tasks;
    }

    /**
     * Constructs a <code>TasksResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TasksResponse} obj Optional instance to populate.
     * @return {module:model/TasksResponse} The populated <code>TasksResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TasksResponse();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('credits')) {
                obj['credits'] = ApiClient.convertToType(data['credits'], 'Number');
            }
            if (data.hasOwnProperty('requester')) {
                obj['requester'] = ApiClient.convertToType(data['requester'], 'String');
            }
            if (data.hasOwnProperty('page')) {
                obj['page'] = ApiClient.convertToType(data['page'], 'Number');
            }
            if (data.hasOwnProperty('page_size')) {
                obj['page_size'] = ApiClient.convertToType(data['page_size'], 'Number');
            }
            if (data.hasOwnProperty('tasks')) {
                obj['tasks'] = ApiClient.convertToType(data['tasks'], ['Number']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TasksResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TasksResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TasksResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['requester'] && !(typeof data['requester'] === 'string' || data['requester'] instanceof String)) {
            throw new Error("Expected the field `requester` to be a primitive type in the JSON string but got " + data['requester']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['tasks'])) {
            throw new Error("Expected the field `tasks` to be an array in the JSON data but got " + data['tasks']);
        }

        return true;
    }


}

TasksResponse.RequiredProperties = ["tasks"];

/**
 * @member {String} name
 */
TasksResponse.prototype['name'] = undefined;

/**
 * @member {Number} credits
 */
TasksResponse.prototype['credits'] = undefined;

/**
 * @member {String} requester
 */
TasksResponse.prototype['requester'] = undefined;

/**
 * @member {Number} page
 */
TasksResponse.prototype['page'] = undefined;

/**
 * @member {Number} page_size
 */
TasksResponse.prototype['page_size'] = undefined;

/**
 * @member {Array.<Number>} tasks
 */
TasksResponse.prototype['tasks'] = undefined;






export default TasksResponse;

